# Итоговый отчёт: UI Testing (Web)

---

## Цель тестирования
Проверить работоспособность, безопасность и стабильность Web-версии мессенджера.

---

## Общий статус

| Категория | Покрыто? | Комментарий |
|----------|----------|-------------|
| ✅ **Smoke UI** (базовый путь) | ✔️ | Вход → чат → сообщение → обновление — работают |
| ✅ **XSS-поведение** | ✔️ | HTML/JS отображается как текст — **не исполняется** |
| ✅ **Граничные случаи** | ✔️ | Кириллица, эмодзи, спецсимволы, 10 000 символов — отображаются, UI не крашится |
| ✅ **Мультипользовательский чат** | ✔️ | Обмен сообщениями между `testuser` и `testuser2` — работает |
| ✅ **Обработка ошибок** | ✔️ | При недоступности бэкенда — **нет краша**, сообщения исчезают (UX-замечание) |
| ⚠️ **WebSocket-функционал** | ⚠️ Частично | — Зависимости (`websockets==15.0.1`) установлены ✅<br>— Соединение `ws://.../notifications/{id}` **принимается** ✅<br>— **Ошибка в обработчике**: `'generator' object has no attribute 'cursor'` ❌<br>→ Баг бэкенда, не инфраструктурный |

---

## Подробности по ключевым тестам

| № | Сценарий | Результат | Статус |
|---|----------|-----------|--------|
| 1 | Регистрация → вход → чат → сообщение | ✅ Все шаги успешны | ✅ |
| 2 | XSS: `<img src=x onerror=alert(1)>` | Отображается как текст, `alert` не срабатывает | ✅ Безопасно |
| 3 | Спецсимволы через UI (Z̷̢̡̧...) | Отображаются как задумано, не крашится | ✅ |
| 4 | Длинное сообщение (10 000 символов) | ✅ Сохранено в БД, в UI — переносится, без горизонтального скролла | ✅ |
| 5 | Чат между двумя пользователями | ✅ Сообщения отправляются и отображаются корректно | ✅ |
| 6 | Отключение бэкенда во время работы | ✅ Приложение не падает, повторяет попытки подключения (включая WS) | ✅ |
| 7 | WebSocket после установки `websockets` | ✅ Соединение принимается, но падает в `crud_message` из-за `conn.cursor()` | 🟥 |

---

## Анализ WebSocket

После установки `websockets==15.0.1`:
- Uvicorn **больше не пишет** `No supported WebSocket library detected` — ✅
- В логах: `WebSocket /ws/notifications/4" [accepted]` — ✅
- Ошибка в `message.py:83`:  
  ```python
  cursor = conn.cursor(dictionary=True)
  # AttributeError: 'generator' object has no attribute 'cursor'